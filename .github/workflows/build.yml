name: Cross-platform build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ── Linux: Vulkan ───────────────────────────────────────────────
  # build-linux:
  #   name: Linux (Vulkan)
  #   runs-on: ubuntu-latest # Это хост, он просто запустит Docker
  #   container: 
  #     image: archlinux:latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Install Dependencies (Arch Linux)
  #       run: |
  #         pacman -Syu --noconfirm
  #         pacman -S --noconfirm --needed \
  #           base-devel \
  #           rustup \
  #           cmake \
  #           clang \
  #           vulkan-devel \
  #           shaderc \
  #           alsa-lib \
  #           libxkbcommon \
  #           wayland \
  #           git \
  #           wget

  #     - name: Fix git safe directory
  #       run: |
  #         git config --global --add safe.directory /__w/Whisper-Real-Time-Transcription/Whisper-Real-Time-Transcription

  #     - name: Setup Rust
  #       run: |
  #         rustup default stable

  #     - name: Verify Environment
  #       run: |
  #         glslc --version
  #         find /usr/include -name "vulkan.hpp"

  #     - name: Build Project
  #       run: |
  #         export CC=clang
  #         export CXX=clang++
  #         # Если ты используешь ort, убедись, что фичи совпадают
  #         cargo build --release --features vulkan --no-default-features

  #     - name: Upload Artifact
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: whisper-rt-linux-vulkan
  #         path: target/release/translator

  # # ── macOS: Metal (Работает — не трогаем) ────────────────────────
  # build-macos:
  #   name: macOS (Metal)
  #   runs-on: macos-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Install dependencies
  #       run: brew install cmake
  #     - uses: dtolnay/rust-toolchain@stable
  #     - uses: Swatinem/rust-cache@v2
  #     - name: Build
  #       run: cargo build --release --features metal --no-default-features
  #     - uses: actions/upload-artifact@v4
  #       with:
  #         name: whisper-rt-macos-metal
  #         path: target/release/translator

  # # ── Windows: CUDA (Исправлено для CMake) ────────────────────────
  # build-windows-cuda:
  #   name: Windows (CUDA)
  #   runs-on: windows-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Install dependencies
  #       run: |
  #         choco install cmake --installargs 'ADD_CMAKE_TO_PATH=System' -y
  #         # LLVM нужен для bindgen, если он используется в зависимостях
  #         choco install llvm -y

  #     - name: Install CUDA toolkit
  #       uses: Jimver/cuda-toolkit@v0.2.19
  #       id: cuda-toolkit
  #       with:
  #         cuda: '12.4.0'
  #         method: network
  #         sub-packages: '["nvcc", "cudart", "cublas", "cublas_dev", "visual_studio_integration"]'

  #     - name: Set CUDA env
  #       shell: powershell
  #       run: |
  #         $cudaPath = "${{ steps.cuda-toolkit.outputs.CUDA_PATH }}"
  #         echo "CUDA_PATH=$cudaPath" >> $env:GITHUB_ENV
  #         echo "$cudaPath\bin" >> $env:GITHUB_PATH
  #         # CUDACXX помогает CMake найти компилятор без гаданий
  #         echo "CUDACXX=$cudaPath\bin\nvcc.exe" >> $env:GITHUB_ENV
  #         # CUDA_TOOLKIT_ROOT_DIR — стандарт для поиска библиотек
  #         echo "CUDA_TOOLKIT_ROOT_DIR=$cudaPath" >> $env:GITHUB_ENV

  #     - uses: dtolnay/rust-toolchain@stable
  #     - uses: Swatinem/rust-cache@v2

  #     - name: Build
  #       env:
  #         CUDA_PATH: ${{ steps.cuda-toolkit.outputs.CUDA_PATH }}
  #       run: cargo build --release --features cuda --no-default-features

  #     - uses: actions/upload-artifact@v4
  #       with:
  #         name: whisper-rt-windows-cuda
  #         path: |
  #           target/release/translator.exe
  #           target/release/*.dll

  ── Windows: CPU (всегда работает) ──────────────────────────────
  build-windows-cpu:
    name: Windows (CPU)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          choco install cmake --installargs 'ADD_CMAKE_TO_PATH=System' -y
          choco install llvm -y

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build
        shell: pwsh
        run: |
          $env:RUSTFLAGS="-C target-cpu=x86-64-v3"
          $env:CFLAGS="/arch:AVX2"
          $env:CXXFLAGS="/arch:AVX2"